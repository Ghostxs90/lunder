#!/usr/bin/env python
# SIMPLE AIMBOT PANEL - CURSED CORE OFFSETS

import os
import sys
import time
import threading
import ctypes
from flask import Flask, render_template_string, request, jsonify
import psutil
from pymem import Pymem
from pymem.pattern import pattern_scan_all
from pymem.memory import read_bytes, write_bytes, read_int, write_int

if sys.platform == "win32":
    try:
        ctypes.windll.user32.ShowWindow(ctypes.windll.kernel32.GetConsoleWindow(), 0)
    except:
        pass

app = Flask(__name__)

# ==================== GLOBALS ====================
SERVER_PORT = 2000
current_process_name = "HD-Player.exe"
aimbot_addresses = []
is_initialized = False
aimbot_active = False
original_values = {}

# ==================== CURSED CORE PATTERN & OFFSETS ====================
NEW_AIMBOT_AOB = "FF FF FF FF ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? FF FF FF FF FF FF FF FF FF FF FF FF ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ??
